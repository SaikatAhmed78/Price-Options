import PropTypes from "prop-types";

const Price = ({option}) => {
    return (
        <div>
            
        </div>
    );
};

Price.PropTypes = {
    option: PropTypes.object
}
export default Price;